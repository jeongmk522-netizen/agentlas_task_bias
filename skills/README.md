# Skills

Skills describe reusable capabilities in the task-bias architecture. They are
not private notes or build notes. They are public operating units that can be
implemented by one or more agents.

Each skill has a `SKILL.md` file with purpose, inputs, procedure, and output.
The folder names are stable so a runtime can map them into its own tool,
prompt, or workflow format.
